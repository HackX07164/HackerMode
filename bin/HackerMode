#!/usr/bin/python3
import os
import sys
from rich import print
sys.path.append(os.environ.get('VIRTUAL_ENV'))
from setup import Installer

try:
    Installer.__getattribute__(sys.argv[1])()
except Exception:
    print(f"""[bold]USAGE:[/bold]
  HackerMode <command>
  
[bold]COMMANDS:[/bold]
 update
 upgrade
 check
""")

